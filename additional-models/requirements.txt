# Dependencies for additional-models/* volatility scripts.
# Tested with Python 3.10 and 3.11 (Prophet requires Python <= 3.11 on
# some platforms; see comments below for caveats).

# Shared
numpy>=1.23
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.6
statsmodels>=0.14
tqdm>=4.65

# Prophet (Meta)
prophet>=1.1.5
# cmdstanpy is pulled in by prophet; pinning a known-good version avoids
# build issues on macOS arm64.
cmdstanpy>=1.2.0

# SilverKite (LinkedIn) -- relatively heavy install; pulls in plotly,
# lightgbm, holidays, dill, etc. Use Python <= 3.10 if pip resolution
# struggles on newer interpreters.
greykite>=1.0.0

# Orbit (Uber)
orbit-ml>=1.1.4

# Transformer (encoder-only PyTorch model)
torch>=2.0
